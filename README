Hello 338!

*********************
How to get the files:
*********************

1) If you have git, use:

    git clone git@github.com:cwru-eecs338/hello338.git

2) Otherwise (e.g. lab computers), use:

    wget --no-check-certificate https://github.com/cwru-eecs338/hello338/tarball/master
    tar -xvf [downloaded-file-name].tar.gz

**************************
What to do with the files:
**************************

1) Have a look at the code and Makefile using your favorite text editor (e.g. vim).

2) Use 'make' to compile the source, producing the executable 'hello338' file.

3) Run the executable using './hello338'

4) Explore how the Makefile works:
   a) 'make clean' -- removes non-source file(s)
   b) 'make dep' -- just prints a message
   c) 'make test' -- prints a message, but has a dependency
